# PDF OCR RAG Accelerator – dependencies
# Notebook 01 installs GPU deps (torch, vllm, ray, flash-attn) via %pip in-notebook.
# The packages below are used by notebook 02 and for local development.

langchain-text-splitters>=0.2.0
databricks-sdk>=0.20.0
PyMuPDF>=1.23.0
